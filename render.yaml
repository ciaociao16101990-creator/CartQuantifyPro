services:
  - type: web
    name: cartquantifypro
    env: node
    plan: free
    branch: main
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    autoDeploy: true
